/*
Note: These values are simply placeholders and may still be changed.
*/

#define   BFS_DEFAULT_QUANTUM     50      // temporary value -- (temp based on Lab 5)
#define   NICE_FIRST_LEVEL        -20     // most negative value, at most 0 -- (temp based on CFS)
#define   NICE_LAST_LEVEL         19      // most positive value, never less than first level

#define   SEED                    140     // for random function