// As per project specifications
#define   BFS_DEFAULT_QUANTUM     50      // arbitrary value -- (based on Lab 5)
#define   NICE_FIRST_LEVEL        -20     // most negative value -- (based on CFS)
#define   NICE_LAST_LEVEL         19      // most positive value

#define   SEED                    6969420 // initial seed for random() in proc.c

// Skiplist specific values
#define LEVELS                    4       // number of levels in the skiplist
#define SKIPLIST_P                2500    // likelihood of placement in a level over 10000
